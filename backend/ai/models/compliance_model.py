from typing import Dict


def score_compliance(dpp_payload: dict) -> Dict[str, str | float]:
    """
    Placeholder for compliance scoring.

    Returns a mock response until AI models are integrated.
    """
    return {
        "score": 87.5,
        "coverage": "Core fields validated",
        "findings": "2 optional fields missing",
    }